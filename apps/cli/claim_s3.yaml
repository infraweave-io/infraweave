apiVersion: infrabridge.io/v1
kind: S3Bucket
metadata:
  name: my-s3bucket
  # namespace: default
spec:
  moduleVersion: 0.0.14 # The released version to use, must match the version in the module.yaml
  variables:
    bucketName: my-unique-bucket-name-3543tea2-playground
    tags:
      Name234: my-s3bucket
      Environment43: dev
