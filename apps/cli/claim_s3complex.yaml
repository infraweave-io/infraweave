apiVersion: infrabridge.io/v1
kind: S3BucketComplex
metadata:
  name: my-s3bucket
  # namespace: default
spec:
  moduleVersion: 0.0.2 # The released version to use, must match the version in the module.yaml
  variables:
