# webapp-example-manual

This example stack uses terraform code to replace the terraform code generated by infraweave.

This should be used as a last restor and not something that is regularly done, since it will mean additional maintenance and also might break compatability with modules in the future.